const myObj = {
    js: 'JavaScript',
    cpp: 'C++',
    rb: 'Ruby',
    swift: 'Swift by Apple',
}

for (const key in myObj) {
    // console.log(`${key} shortcut is for ${myObj[key]}`);
}


// for Array

const pro = ['JavaScript', 'C++', 'Ruby', 'Swift by Apple'];

for (const key in pro) {
    console.log(pro[key]);
}